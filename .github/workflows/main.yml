name:Basic CI orkflow
on:
push:
branches:["main"]
pull_request:
branvhes:["main"]
jobs:
build:
runs_on :ubuntu_latest
steps:
-name:checkout repository
uses:action/checkout@v3
-name:Display a message
run:eco"Helollo,Github Action!"
